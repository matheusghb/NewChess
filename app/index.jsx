import { StyleSheet, Text, View } from 'react-native' // importa os elementos da tela.
const Home = () => { // cria uma função que será usada pra renderizar os elementos.
    return (
        <View>
            <Text>Home</Text>
        </View>
    )
}
export default Home
const styles = StyleSheet.create({

})

